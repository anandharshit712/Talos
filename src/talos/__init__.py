"""Talos -- multi-agent system for attack detection, classification, and scope analysis.

The package root deliberately holds nothing but this docstring and the version (R1.4).
Configuration lives in ``talos.core.settings``, data contracts in ``talos.schemas``, and the
extension interfaces in ``talos.core.agent_contracts``.
"""

__version__ = "0.1.0"
